import SchedulerForm from "../components/SchedulerForm";

function FormPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Scheduler Form</h1>
      <SchedulerForm />
    </div>
  );
}
export default FormPage;
// This is a simple form page that imports and uses the SchedulerForm component.